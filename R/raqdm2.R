aqdm_url <- "https://aqs.epa.gov/api" # Don't use a trailing /

validation <- list(
  list = list(name = c("state", "county", "site", "pc", "param", "format", "cbsa", "csa", "duration", "agency"),
              dots = c("state", "county", "pc", "agency_code"))



)
